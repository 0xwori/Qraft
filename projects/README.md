# Qraft Projects

Projects keep Codex work separated and clear.

Use one folder per project:

```text
projects/<project-id>/
```

Each project can have its own context, assets, scripts, MCP templates, tools, outputs, and AI logs.

Run:

```bash
npm run qraft:project -- list
npm run qraft:project -- show tapwise
```
